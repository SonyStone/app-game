# Solid SVG Editor TODO

The current extensibility audit lives in `../ARCHITECTURE.md`.

Keep this file for short implementation notes only. The roadmap should stay at the app root so it
is visible beside `package.json`, test/build configuration, and future package-level docs.
