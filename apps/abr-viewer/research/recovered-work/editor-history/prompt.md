# Prompt: Disassemble Photoshop .abr Files

Your task is to create a TypeScript script that can read and extract all data from Photoshop .abr files. Before writing the script, you need to research the `.abr` file format and create a schema. To achieve this, follow these steps:

1. **Research the `.abr` File Format**:
   - Use any available resources, tools, or documentation to understand the structure of `.abr` files.
   - Install and use any disassembly tools or applications necessary to inspect the contents of `.abr` files.
   - Load the provided `.abr` files and analyze their structure to extract details about brush settings, brush images, and metadata.

2. **Create a Schema**:
   - Based on your research, define a schema that represents the structure of `.abr` files.
   - Include details such as file headers, brush properties, and image data.

3. **Write the TypeScript Script**:
   - Use the schema to implement a TypeScript script that can parse `.abr` files.
   - Ensure the script extracts all relevant data, including brush settings, images, and metadata.
   - Include comments and documentation for clarity.

### Deliverables:
- A detailed schema for `.abr` files.
- A TypeScript script that fulfills the above requirements.
- Instructions on how to run the script and test it with `.abr` files.

Feel free to use any resources or references to understand the `.abr` file format and implement the solution.