# Virtual scrolling experiments

These files preserve earlier comparison work but are not part of the active
examples or the published package surface.

- `FlattenedList.tsx` records the alternative flat-index rendering experiment.
- `primitives-virtual-list-example.tsx` preserves the earlier
  `@solid-primitives/virtual` comparison.
- `useVirtualMap.ts` records the original recursive height-map sketch.
