import styles from './BlockTree.module.css';

/** Local class used by tree blocks and placeholders. */
export const blockClass = styles.block;
/** Local class used by measurement spacers. */
export const spacerClass = styles.spacer;
/** Per-tree transition duration custom property. */
export const durationVar = '--solidnest-duration';
/** Per-container block spacing custom property. */
export const spacingVar = '--solidnest-spacing';
