export const VENDOR_PREFIXES = ['', 'webkit', 'Moz', 'MS', 'ms', 'o'];
export const TEST_ELEMENT = document.createElement('div');

export const TYPE_FUNCTION = 'function';

export const { round, abs } = Math;
export const { now } = Date;
