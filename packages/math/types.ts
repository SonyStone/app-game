import { Branded } from '@packages/utils/branded.type';

/**
 * A type representing a value in radians.
 */
export type Radians = Branded<number, 'Radians'>;

/**
 * A type representing a value in degrees.
 */
export type Degrees = Branded<number, 'Degrees'>;
