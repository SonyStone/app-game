# Paint Studio

Рабочая первая версия редактора находится на `/paint/studio`. Старые эксперименты остаются в `/paint`.

## Возможности

- Растровый холст с разреженными тайлами 256×256 и отрицательными координатами.
- Мягкая круглая кисть, ластик, размер, hardness, независимые flow и opacity целого мазка.
- Давление пера отдельно управляет размером и flow. Мышь использует давление 1.
- Слои, видимость, порядок, непрозрачность, Smooth color / Normal (classic) / Multiply / Screen / Overlay.
- Холст занимает всё окно. Компактные плавающие инструменты используют стиль `/grease-pencil-typegpu`; настройки кисти, цвета, слоёв и файлов открываются по запросу. На телефонах панели располагаются снизу, в низком окне прокручиваются.
- Undo/redo по точным снимкам изменённых тайлов, включая изменения слоёв.
- Pan, zoom, rotate и зеркальное отображение камеры. Puck вызывается удержанием Space, клавишей V или правой кнопкой.
- Автосохранение завершённых действий в IndexedDB, восстановление после перезагрузки, импорт/экспорт `.paint`.
- Drawing menu → Canvas wireframe показывает занятые тайлы, две треугольные грани каждого тайла и расход памяти. Overlay учитывает pan/zoom/rotate/mirror и текущий мазок, не попадает в PNG или файл проекта.
- PNG экспортирует текущий вид холста с фоном. Это не экспорт всего бесконечного документа.
- Потеря GPU-устройства приостанавливает рисование. Кнопка восстановления создаёт новое устройство из сохранённых в памяти пикселей завершённых мазков.

## Управление

| Действие               | Управление                                  |
| ---------------------- | ------------------------------------------- |
| Рисовать               | Перо или левая кнопка мыши                  |
| Переместить вид        | Зона Pan в Puck, средняя кнопка или один палец |
| Масштаб и поворот      | Два пальца                                  |
| Масштаб                | Колесо, кнопки сверху или puck              |
| Puck                   | Удерживать Space, V, правая кнопка или кнопка справа внизу |
| Кисть / ластик         | B / E                                       |
| Размер кисти           | [ / ]                                       |
| Отмена / повтор        | Ctrl или Cmd + Z / Shift + Z                |
| Сохранить файл         | Ctrl или Cmd + S                            |
| Отменить текущий мазок | Escape                                      |

Касания во время рисования пером игнорируются. Это простое подавление случайных касаний ладонью, а не классификатор контактов планшета.

Puck использует векторную версию предоставленного пользователем изображения (`packages/navigation-puck/src/assets/navigation-puck.svg`); исходный PNG сохранён рядом как образец: синее кольцо поворачивает вид, центр перемещает, нижняя секция меняет масштаб. UI, контроллер и обработчики ввода находятся в общем пакете `@app-game/navigation-puck`: Puck скрывается на время жеста, zoom реагирует только на вертикальное перемещение, масштабирование и поворот используют центр viewport. При разовом вызове Puck закрывается после операции. При удержании Space он снова появляется в точке отпускания; отпускание Space посреди жеста позволяет завершить жест. Правый drag выбирает изображённую зону после 30 px движения, при выборе правым drag вокруг центра действует мёртвая зона 12 px; прямое нажатие на кнопку начинает жест сразу. Shift фиксирует поворот с шагом 15°. Зоны также поддерживают стрелки клавиатуры; крестик закрывает Puck. Панели закрываются крестиком, нажатием снаружи или Escape.

## Устройство

`PaintStudio.tsx` собирает интерфейс из `BrushPanel`, `LayersPanel` и общего `NavigationPuck`. `paintNavigation.ts` адаптирует общие жесты к 2D-камере Paint. `createPaintSession.ts` управляет worker и DOM-слушателями. `input.ts` получает Pointer Events, сохраняет coalesced samples с их давлением и отправляет один пакет за animation frame. Перед `end` ожидающие samples отправляются обязательно.

Worker содержит Solid root для жизненного цикла, модель документа и GPU renderer. `brush.ts` размещает отпечатки по длине пути, сохраняя остаток шага между пакетами. JSX используется для крупных модулей интерфейса; отдельные отпечатки представлены массивами, а не реактивными компонентами.

Каждый затронутый тайл имеет исходное изображение, маску мазка и отдельный результат. Эти GPU-текстуры переиспользуются, пока тайл находится в кэше. Новый мазок копирует исходное изображение и очищает маску на GPU. Маска накапливает flow аппаратным source-over blending. Шейдер смешивает исходное изображение с маской, применяя opacity один раз ко всему мазку. Ластик использует destination-out. На завершении только затронутые тайлы читаются в CPU и становятся одной записью истории. Резидентные тайлы читаются одной отправкой копирования и одним mapAsync, без последовательного ожидания каждого тайла.

Тайлы хранят premultiplied RGBA8 в sRGB. Режим Smooth color декодирует цвета в линейный RGB, выполняет source-over и кодирует результат обратно в premultiplied sRGB. Кисть и слои используют общую TypeGPU-функцию `gpu/colorMixing.ts` с [передаточной функцией sRGB из W3C](https://www.w3.org/TR/css-color-4/#color-conversion-code). Режим включён по умолчанию для кисти и новых слоёв. Это устраняет провал яркости при смешивании красного с зелёным; это не физическая модель пигментов.

Существующие файлы сохраняют выбранные режимы. Для старого слоя можно выбрать Smooth color в панели Layers. Multiply продолжает затемнять цвет согласно своему назначению. Уже записанные в растровые тайлы тёмные пересечения старых мазков не пересчитываются. ICC, HDR и 16-bit painting пока не реализованы.

Камера не изменяет пиксели. В GPU передаются смещения тайлов относительно центра камеры, чтобы сохранять точность далеко от начала координат. Рендерер отсекает невидимые тайлы и группирует их в render passes. Mipmaps обновляются только при уменьшении изображения, когда они нужны для выборки. Слои смешиваются через две текстуры размером с viewport и промежуточную текстуру слоя. Четвёртая текстура сохраняет готовый вид: при рисовании пересчитывается лишь экранная область изменённых тайлов, включая все слои в этой области. Навигация, изменения слоёв, undo/redo и отмена мазка инвалидируют вид. Очистка выполняется через render-pass loadOp, без массивов нулей в CPU. В простое новые кадры не отправляются. Worker ожидает завершения GPU-кадра перед следующим запланированным кадром и объединяет поступившие изменения камеры, чтобы очередь отрисовки не накапливалась.

## Пределы первой версии

- Нужны WebGPU и OffscreenCanvas. Автоматического WebGL2 fallback пока нет.
- Кэш рисования содержит до 128 тайлов со вспомогательными текстурами мазка. При вытеснении активного тайла его маска и результат сохраняются в CPU. Отдельный кэш отображения ограничен 96 MiB плюс одна временная текстура 256×256 с mipmaps. Он хранит только уровни детализации, необходимые при текущем масштабе. При приближении более подробные пиксели загружаются из исходного документа.
- Бюджет документа — 256 MiB фактически сохранённых байтов тайлов, до 65 536 тайлов и 128 слоёв. Пустые RGBA-пиксели сжимаются без потерь; плотные тайлы остаются raw. GPU разворачивает только нужные тайлы, включая вытесненные маски текущего мазка. Координаты холста не ограничены размерами окна.
- История ограничена 100 действиями и 64 MiB снимков. Действие, которое само превышает этот объём, не остаётся в истории.
- Разрешение viewport ограничено DPR 2, лимитом GPU-текстуры и примерно 8 мегапикселями. Разрешение документа от этого не меняется.
- Mipmaps сейчас существуют на уровне отдельных тайлов. Иерархические обзорные тайлы для отдаления очень больших документов ещё не реализованы. Навигация использует отдельный кэш отображения и не вытесняет ресурсы кисти. При превышении бюджета отображения на новом масштабе применяется LRU; иерархические обзорные тайлы остаются следующим этапом. Рисование при неподвижной камере обновляет локальную область сохранённого вида.
- История не сохраняется между перезагрузками. Автосохранение отражает завершённые действия; незавершённый мазок не входит в checkpoint.
- Нет выделений, масок слоёв, фильтров, текстурных кистей, PSD, ICC или совместного редактирования.

Это ограничения текущей реализации, а не отмена направления развития. Следующие этапы: обзорные тайлы, более богатые кисти и стабилизация пера, маски/выделения, полноценный формат проектов и расширенный цветовой pipeline.

## Проверка

Из корня репозитория:

```sh
pnpm --filter @app-game/paint typecheck:studio
pnpm --filter @app-game/paint test:studio
pnpm --filter @app-game/paint build:studio
```

Production build находится в `packages/paint/dist-studio`. Он использует тот же компонент редактора и тот же worker, что `/paint/studio`, без зависимости от остальных экспериментов playground.

При запущенном общем dev server откройте `/paint-studio-qa.html`. Кнопки запускают проверки реального GPU и production worker. Они используют отдельные документы и отдельную временную базу IndexedDB, не изменяя автосохранение редактора.

GPU-проверки сравнивают пиксели на стыке тайлов, opacity и стирание, результат разных группировок отпечатков, вытеснение активной маски, четыре классических режима смешивания и восстановление после потери устройства. Отдельная проверка Smooth color измеряет красно-зелёное смешивание мазков в обоих порядках и смешивание двух слоёв: при равном вкладе ожидается RGB (188, 188, 0). Worker-проверки проходят рисование, историю, portable-файл, IndexedDB, повторный запуск worker, import и PNG export.

GPU-проверки также проверяют отсутствие новых текстур и CPU-загрузок при повторных мазках по резидентным тайлам, единый mapAsync, отложенные mipmaps и точное совпадение частичной/полной перерисовки на 196 фоновых тайлах при кэше на 16. Кнопки `Measure 60 strokes` и `Measure large document` запускают повторяемые замеры на отдельных документах. Второй замер сравнивает принудительную полную перерисовку и обновление изменённых областей на документе больше кэша. Время завершения мазка включает ожидание GPU и чтение пикселей; это не задержка реального пера.

Локальный прогон 2026-09-05: 60 мазков, 220 тайлов, два слоя, масштаб 25%, кэш 128 тайлов, viewport около 8,4 мегапикселя. Обе стратегии измерены на исправленном рендерере; полная перерисовка принудительно инвалидирует сохранённый вид перед каждым кадром.

| Метрика                              | Полная перерисовка | Изменённые области |
| ------------------------------------ | -----------------: | -----------------: |
| Среднее CPU-время render             |           59,93 мс |            0,87 мс |
| p95 CPU-времени render               |           79,70 мс |            2,10 мс |
| Среднее завершение мазка с readback  |           17,39 мс |            3,72 мс |
| CPU → GPU загрузки текстур за прогон |        3504,25 MiB |             12 MiB |

Эти числа описывают синтетическую нагрузку на локальном устройстве. Прогон не измеряет DOM-ввод, задержку физического пера или автосохранение. Автосохранение пока записывает полный checkpoint; инкрементальная запись проекта остаётся отдельной задачей.

Показатель `ms submit` в меню Drawing измеряет CPU-время подготовки/отправки кадра. Он не измеряет GPU-время или задержку от пера до экрана. Для них нужны отдельные измерения на целевых устройствах.


### Навигация, 2026-09-05

`Measure navigation` на `/paint-studio-qa.html` измеряет 60 кадров pan/zoom/rotate на 196 тайлах, viewport 1200×1800 CSS px, DPR 2 с ограничением около 8,4 мегапикселя. После прогрева каждый кадр ожидает `onSubmittedWorkDone`, поэтому завершённое время включает CPU и GPU, но не DOM-ввод или задержку физического пера.

| Метрика | Общий кэш кисти/отображения, до исправления | Отдельный кэш отображения |
| --- | ---: | ---: |
| Среднее CPU-время кадра | 44,82 мс | 0,60 мс |
| Среднее время до завершения GPU | 66,92 мс | 1,68 мс |
| p95 времени до завершения GPU | 80,70 мс | 2,30 мс |
| CPU → GPU за 60 кадров | 2940 MiB | 0 MiB |
| Создано текстур за 60 кадров | 11760 | 0 |

Это локальный синтетический замер. Холодный кэш и приближение, требующее более подробных тайлов, всё ещё требуют загрузки. GPU-регрессия сравнивает каждый пиксель с исходным рендерером при разных масштабах, pan/rotate/mirror, рисовании, стирании и undo. Отдельно проверен обзор всех 1024 тайлов без повторной загрузки и с соблюдением бюджета памяти.

Формат `.paint` версии 2 хранит сжатые тайлы; загрузчик продолжает читать raw-файлы версии 1. `tilePixels.ts` сохраняет все байты premultiplied RGBA8, включая мягкую прозрачность. GPU-регрессия `verifySparseStroke` проводит непрерывный мазок через 1101 тайл: 275.25 MiB raw → 34.40 MiB сохранённых пикселей; проверяет края мазка, undo/redo и отображение при 5% zoom.
